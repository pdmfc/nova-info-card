const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  theme: {
    extend: {},
    screens: {},
    colors: {
      red: colors.red,
      yellow: colors.yellow,
      green: colors.green,
      blue: colors.blue
    }
  },
  variants: {},
  plugins: []
};
